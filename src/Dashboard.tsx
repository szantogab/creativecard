import * as React from 'react';
import './App.css';

export default class Dashboard extends React.Component<{}, {results: any[]}> {
    render() {
        return (
            <div>
                <h1>Dashboard</h1>
            </div>
        );
    }
}
